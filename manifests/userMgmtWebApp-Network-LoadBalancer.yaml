apiVersion: v1
kind: Service
metadata:
  name: usermgmt-webapp-nlb-service
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: nlb
spec:
  type: LoadBalancer
  selector:
    app: usermgmt-webapp
  ports:
    - port: 80
      targetPort: 8000